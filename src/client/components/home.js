import React from "react";

const Home = () => {
  return (
    <div>
      <div>I'm the homeee page</div>
      <button onClick={() => console.log('fweg')}>Press</button>
    </div>
  );
};

export default Home;
